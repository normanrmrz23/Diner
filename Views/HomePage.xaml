<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Diner.Views.HomePage"
             xmlns:viewModels="clr-namespace:Diner.ViewModels"
             Title="Home"
             BackgroundColor="#526534">
        <VerticalStackLayout
            VerticalOptions="Center"
            Spacing="80">
            <VerticalStackLayout
                Padding="0,40,0,0"
                Spacing="0">
            <Label
                Text="DINER"
                FontFamily="AltaRegular"
                FontSize="50"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                TextColor="#C1B6A4"/>
                <Label></Label>
            <Label
                Text="collect and share restaurant experiences"
                FontSize="14"
                HorizontalOptions="Center" />

            </VerticalStackLayout>
        </VerticalStackLayout>
</ContentPage>